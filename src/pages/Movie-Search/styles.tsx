import { Alert } from '@mui/material';
import styled from 'styled-components';

export const MovieSearchContainer = styled.div`
  display: block;
  justify-content: space-evenly;
  align-items: center;
  width: 100%;
`;

export const SearchAlert = styled(Alert)`
  width: 400px;
  margin: 0 auto;
`;
